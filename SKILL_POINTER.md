# V14 SIC Deployment Protocol — Skill Pointer

> **This is a pointer, not the protocol.** The full V14 protocol lives on GitHub.

## INSTRUCTIONS

Before doing ANYTHING on any SIC HB1000 initiative, you MUST:

1. **Clone the live repo**:
   ```
   gh repo clone timjlatimer/v14-sic-deployment-protocol
   ```

2. **Read SKILL.md from the cloned repo** — that is the single source of truth. It contains the full deployment sequence, all standing directives, the 5-layer defense system, the preservation architecture, and every constitutional file.

3. **Follow the ORIENT phase** in SKILL.md before proceeding with any work.

4. **At the end of every task**, push all changes back to the GitHub repo. This is a hard gate — the task is not complete until the push is confirmed and the heartbeat is written.

## WHY THIS EXISTS

The full protocol lives on GitHub so that:
- Every chat reads the LATEST version (no stale copies)
- Every chat writes BACK to the repo (the library grows, never shrinks)
- V14 can improve itself without manually updating every skill file
- The complete audit trail lives in git history

## TRIGGER PHRASES

"deploy V14", "new initiative", "start a new project", "franchise kit", "run the protocol"

## REPO LOCATION

`github.com/timjlatimer/v14-sic-deployment-protocol`

## IF THE REPO IS UNAVAILABLE

If you cannot clone the repo (network issue, permissions), STOP and notify Tim immediately. Do NOT proceed without the constitutional memory. The whole point of V14 is that no task runs without the constitution loaded.
